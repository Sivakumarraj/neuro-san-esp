# For all the available configuration options, please view the following file:
#
# https://github.com/DavidAnson/markdownlint/blob/main/schema/.markdownlint.yaml
#

plugins:

  # MD007/ul-indent : Unordered list indentation : https://github.com/DavidAnson/markdownlint/blob/v0.38.0/doc/md007.md
  MD007:
    indent: 4

  # MD013/line-length : Line length : https://github.com/DavidAnson/markdownlint/blob/v0.38.0/doc/md013.md
  MD013:
    # Number of characters
    line_length: 120
    # Exclude tables
    tables: false
    # Exclude fenced code blocks 
    "code_blocks": false


  # MD033/no-inline-html : Inline HTML : https://github.com/DavidAnson/markdownlint/blob/v0.38.0/doc/md033.md
  "MD033": false

# GitHub tables are an extension in pymarkdown, off by default. Without it the
# `tables: false` exemption above never applies, because nothing is recognised
# as a table and every long row reads as a long line of prose.
extensions:
  markdown-tables:
    enabled: true
